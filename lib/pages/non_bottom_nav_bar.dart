
import 'package:flutter/material.dart';

class NonBottomNavBarPage extends StatelessWidget {
  const NonBottomNavBarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Non Bottom Nav Bar Page"),
      ),
    );
  }
}